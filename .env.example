# Copy to .env and fill in real keys. .env is gitignored.
# Do NOT store this file with real keys in a cloud-synced folder.
VIRUSTOTAL_API_KEY=your_virustotal_key_here
ABUSEIPDB_API_KEY=your_abuseipdb_key_here
# Optional (GreyNoise community works without a key, just rate-limited):
GREYNOISE_API_KEY=
